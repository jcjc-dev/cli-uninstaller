{
  "id": "claude-code",
  "name": "Claude Code",
  "install_commands": [
    "curl -fsSL https://claude.ai/install.sh | bash",
    "irm https://claude.ai/install.ps1 | iex"
  ],
  "installer_artifacts": [
    "~/.local/bin/claude",
    "~/.local/bin/claude.exe",
    "~/.local/bin/claude.cmd",
    "~/.local/share/claude",
    "~/.cache/claude"
  ],
  "user_data": [
    "~/.claude",
    "${XDG_CONFIG_HOME:-~/.config}/claude"
  ],
  "notes": [
    "The native installer places the claude binary under ~/.local/bin according to vendor help and docs.",
    "State directories are intentionally treated as purge-only because they can include auth, settings, commands, transcripts, memories, and session history."
  ]
}
